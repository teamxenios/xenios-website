# Production account and partner schema read — 2026-09-05

This is a read-only observation of the linked Supabase project
`yvzeduaxbwgcwllhywff`, captured at `2026-09-05T22:43:06.5168+00:00` using
the repository's local Supabase CLI. The transaction was read-only and the
query returned schema, constraints, indexes, policies, function definitions and
aggregate row counts only. No identity, customer, partner, evidence or payment
rows were exported.

The live database has the canonical `research_applications`,
`research_members`, `research_partners`, partner agreement/training/lifecycle,
notification, attribution, idempotency and rate-limit tables. It does not yet
expose the approved-customer authority or partner-lifecycle authority functions.
The referral V1 tables/functions are also absent, while its canonical extension
tables (`research_partner_links`, `research_attribution_touches` and
`research_idempotency_keys`) are present. Therefore the new account and partner
SQL candidates remain unapplied release prerequisites; referral V1 remains a
separately gated candidate.

The current member constraints still allow only historical `paid_membership`
and `sponsored_b2b` access bases, and the application status constraint does not
include `approved_customer`. These facts are the expected pre-apply state for
the approved-customer candidate and must be reconciled by its exact precheck.
The current partner schema includes `certified_by_admin_id`; the read projection
requires both a past certification timestamp and this actor provenance before
showing the certification gate as satisfied.

Machine-readable schema-only evidence is in
[`production-account-partner-schema.json`](./production-account-partner-schema.json),
generated by [`read-account-partner-schema.py`](C:/Users/sboad/.codex/tmp/read-account-partner-schema.py)
from [`read-account-partner-schema.sql`](../../../../scripts/revenue-launch/read-account-partner-schema.sql).
Its LF SHA-256 is `9254b6d9b7e8717c42b23e9752cb2eaccf90d43d9f1812d77eac44990d21cf77`.
The read script is intentionally outside the repository because it obtains a
short-lived local CLI credential context; it writes only the screened schema
artifact. Re-run it before any production proposal because this observation can
become stale.

No production migration, configuration change, deployment, Auth creation,
customer approval, partner operation, email or referral action occurred.
