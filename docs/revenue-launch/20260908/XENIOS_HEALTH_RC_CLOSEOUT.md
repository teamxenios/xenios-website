# Xenios Health release candidate — local closeout, 2026-09-08

Claude (temporary local integrator, founder-directed during the Codex outage).
Every result below was measured on the pinned Node 20.19.0 from a clean
checkout; nothing was skipped, suppressed, retimed or relabelled to pass.
**No production deployment, migration, flag, account or notification was
performed.** Claude has not inherited any executor-specific production GO.

## SOURCE / TREE

| Identity | Value |
| --- | --- |
| Candidate head | `45f95dfe51ef0aa226b39413c1fbd02fc121ece8` (tree `29684232649d66de9b56316753aa2879c53ec93d`) on `claude/xenios-health-rc-20260908` |
| Runtime freeze | `bf7b5fee78102289bcc6c68e9e336bb0ea0c9d5e` (tree `d7a53f2a074eb8c963344d415d55eb75dff34936`); the candidate adds only `docs/revenue-launch/20260908/` on top of it |
| Production base (live) | `ff3c496245739233b71e46f9e5d6e26af9d57017`, Render deploy `dep-dafcm567bikc7382rhng` |
| Range | 62 runtime commits, 190 changed files (client 90, docs 53, server 17, `.xenios` 12, scripts 7, supabase 6, shared 3, package.json, package-lock.json) |

## FULL-SUITE EVIDENCE BASIS

`46782cd4f0f73975c021c3a605492aa022c7d4dc` (tree `6e7965d91ba4a01e416bcc1a7485b0cde80acf27`):
**917 files passed / 5 skipped (922); 15,997 tests passed / 59 skipped
(16,056); 0 failures; 0 unhandled errors; 314.21 s**, idle host, clean tree.

Equivalence to the candidate, stated exactly: `46782cd..bf7b5fe` touches five
files — four documentation/handoff records and one trailing blank line removed
from `scripts/revenue-launch/new-account-browser-qualification.mjs` (5 files,
+5/−8); `bf7b5fe..45f95dfe` is documentation only. No executable source,
migration or configuration differs. The suite was **not** rerun at the newer
commits, and this record does not pretend it was.

## BUILD / TYPECHECK

At the runtime freeze: `tsc` exit 0, 0 errors (2026-09-08T01:56:22Z);
`check:release-control-plane` exit 0; `script/build.mjs` exit 0
(2026-09-08T01:57:23Z), client bundle built, `dist/index.cjs` 1,674,425 bytes.
`git diff --check ff3c496..45f95dfe` exit 0 (02:00:49Z).

## MIGRATION REHEARSAL

`scripts/revenue-launch/rehearse-resource-library.mjs` on the external PGlite
install A's records name: **154 checks, PASS**, 2026-09-08T01:19:52.597Z
(`resource-hub-rehearsal-bf7b5fe.json`). Source hashes (canonical Git blobs,
LF-identical):

| File | SHA-256 |
| --- | --- |
| `…research_resource_library.sql` (candidate and `supabase/migrations/` copy are byte-identical, 11,531 bytes) | `e55f965fbc942b4de6ec7b74b2bc28b8d7eff8dd6530f50c9876d3137e8dd722` |
| `….precheck.sql` | `742bce5729a0b4c2db848fbf32658efe0b286c5b910730fd6bfc9182c1ea6fab` |
| `….postcheck.sql` | `7c7ef33b7091cc1946077070ca6587491b299c5101ca05b4b70dcf95ff379550` |
| `….rollback.md` | `1c8261fbb1723c7ef0d47fdd6a8f65d150ff7931ee8d3b2f896b134a8dea4153` |

`MIGRATION_DAG.json` carries the same checksum, `appliedToProduction: false`,
`managedMigrationId: PENDING`. Managed PostgreSQL 17.6 parity, Storage HTTP
behaviour and concurrent-session locking remain unproven locally, exactly as
A's qualification record says.

## BROWSER ACCEPTANCE

B's independent rendered run at `8e125ca7` (tree `56d76664…`): **14 journey
steps, 45 captures, exit 0**, 2026-09-07T23:12:45Z; report SHA-256
`f241886c7ef734594e4ceda52ba87c856775834d8d8f2729e963152bab31d2ad`, verified
present. Since `8e125ca7` the only runtime-adjacent changes are
`server/release-control-plane.test.ts` and the Resource Hub SQL files, none of
which the loopback preview serves or executes. **Not rerun by Claude.** B's
stated limits stand: synthetic auth and storage; QR/referral activation,
commerce, recruiter workflows and real onboarding are outside that proof.

**Affiliate links and QR export — rendered, 2026-09-08T02:31Z (Claude's own
run, not an independent review):** 21 steps, 0 failures on the candidate's
built client — sign-in with exact return, the real referral route and store
over a fake RPC, print card whose SVG equals an independently generated QR,
**jsQR decode of the rendered code back to the exact URL**, a byte-identical
SVG download, no overflow at five widths, rendered sign-out, a no-partner
negative control, and a 401 without a session. `QR_LINKS_WALKTHROUGH.md`
carries scope, hashes and what it does not prove.

## MANIFEST / OWNERSHIP REVIEW

Schema-version-2 manifest with `integrationOwnershipReview` generated by
`build-rc-manifest.mts` from `rc-inputs.json` (both beside this file) and
verified with `verify-release-manifest.ts` under explicit trusted identity:

```
Release manifest accepted: docs/coordination/release-manifests/XENIOS_HEALTH_RC_2026-09-08.json   (2026-09-08T02:03:56Z)
```

Negative control with the wrong expected head: refused (`HEAD_SHA_MISMATCH`).
Recomputed from the trusted base policy at `ff3c496`
(`67deb85b0a5eb3d48c58d7382502c7484674a620e41d1d8cd06995a417291fe3`, 48 rules):
190 files, **167 UNOWNED_FILE, 0 WRONG_LANE_OWNER, 0 OWNERSHIP_CONFLICT** —
reconcilable by the review mechanism; nothing conflicting.

**The verified run used a draft reviewer identity that was then deleted. No
manifest or review artifact is committed.** The mechanism requires a real
reviewer; Claude is the author and will not name itself. The reviewer's packet
is `ownership-findings-ff3c496-45f95dfe.json` (the 167 paths by area, and the
23 already owned by the `release-manager` lane). On acceptance, one command
regenerates and commits the manifest with the reviewer's identity and time.

## SECRET SCAN

`scan-release-diff.mjs` over the exact range: 102 raw
"generic assigned secret" matches, every one in a test, preview or harness
file, none in runtime code. With B's accepted registry bound: **102 reviewed,
0 unresolved** (measured at `46782cd`, registry `0d19ecfc…`). At `bf7b5fe` and
at the candidate the loader **refuses** the registry — `bf7b5fe`'s whitespace
cleanup changed one pinned fixture file by one trailing blank line. Proven
delta: 30 vs 29 added lines, the only difference the empty string; every
reviewed match hash still present. A re-bound registry sits at
`docs/revenue-launch/20260908/` with status `PENDING_REACCEPTANCE`, which the
loader refuses by design; see `SYNTHETIC_CREDENTIAL_REBIND_REQUEST.md`. B's
accepted originals are untouched. The strict wrapper was not run because it
also requires the names file below.

## APPROVED-NAME INPUT

**Missing.** Identity: 6,463 bytes, SHA-256
`c7da9838a3a8236a1b94465f3bfe21121fcc0de9dc586f8c08e167cf7d9ac34b`. No
filename, path, generation procedure or backup reference exists anywhere in
the repository or in A's recovery receipts — only the hash and size. The
2026-08-29/30 gate ran inside a container with the corpus mounted from an
unrecorded host path. A searched Downloads (657,316 files) and AppData Temp
(113,791) with zero exact matches; Claude's profile-wide size-and-hash scan was
stopped as unbounded. Recovery or an explicitly approved replacement version is
a founder action; no substitute was created.

## PII SCAN

**Not run.** Nothing about the release's personal-information exposure is
claimed.

## REMAINING EXTERNAL ACTION

1. Supply the original approved-name corpus (hash-verified) or approve a
   documented replacement as a new input version.
2. A reviewer other than Claude accepts the fixture re-bind at `bf7b5fe`
   (one trailing blank line) so the strict scan can bind.
3. A reviewer other than Claude accepts the integration ownership review
   (167 unowned paths, 0 conflicts) so the manifest can be committed.
4. Exact-SHA production approval naming the executor, the migration hash
   `e55f965f…`, flags (`RESEARCH_RESOURCE_HUB_ENABLED` stays false), rollback
   and the live verification steps.
5. Render and Supabase execution access for whoever is named executor; this
   session's Render connector is unauthenticated and its Supabase connector
   rejected its token. Exact requirements, the pattern `ff3c496` used, and
   what unblocks each connector: `EXECUTOR_ACCESS_REQUIREMENTS.md`. Until
   both are green, Claude cannot be the executor.

## EXACT RELEASE SCOPE

**Included** (everything in `ff3c496..45f95dfe`, measured from the diff):
Resource Hub Slice 1 — `server/research/resource-hub` (10 files), the five
admin doors, the partner Resources page, the PDF scanner with A's tokenizer
and strict policy, principal-bound client operations
(`client/src/research/resource-hub`, 4 files), the locked-gate member
admission and signed download door in `server/research/index.ts`; affiliate
and partner page work — 28 files under `client/src/research/pages/partners`,
4 under `pages/member`, 2 under `pages/adminx`, the account-portal views (4),
the recommendation QR export (`recommendation/qr-export.ts` with
`qrcode-generator@2.0.4` and `jsqr@1.4.0` added to `package.json`) and
`shared/research/auth-return-to`; release tooling (reviewed-fixture
registry, release diff scan, pgcrypto guard, control-plane pins); the
flag-gated Resource Hub migration (unapplied); records and evidence.

Of these, B's rendered browser proof covers the Resource Hub journeys. The
QR export and the Links page have Claude's rendered walkthrough
(`QR_LINKS_WALKTHROUGH.md`); all fourteen partner pages have Claude's
rendered empty-state sweep at 1440 and 320 px (`PARTNER_PAGES_SWEEP.md`),
neither independently reviewed. That sweep surfaced **two small responsive
defects** in reviewed code: at 320 px the Training page overflows by 50 px
(module cards lay out at 354 px; still +10 px at 360) and the Support page
by 9 px (the nowrap "Email team@…" button). Both fit at 390 px. Source is
frozen, so they are recorded for the owner's disposition, not fixed. Nothing
about QR or referral activation is claimed live.

**Excluded**: `scripts/resource-hub/strip-embedded-attachments.mjs` and kit
groundwork (outside the candidate); any activation of
`RESEARCH_RESOURCE_HUB_ENABLED`; real content upload, partner activation,
prices, payments, supplier facts, fulfilment, tracking, recruiter or
organization workflows — none is represented as live by this candidate.

## Two milestones, not one

This candidate deploys the Resource Hub **dark**. `RESEARCH_RESOURCE_HUB_ENABLED`
stays absent, so after deployment affiliates see an empty library and admins
cannot upload. Activation — flag, approved content, intended users, live
access and download checks — is a **second approval**, specified in
`DEPLOYMENT_VS_ACTIVATION.md` beside this file. Neither milestone delivers
prices, payments, fulfilment, recruiter, sales-AE or organization workflows.

## Proposed approval request for Milestone 1 (not yet requestable)

When items 1–3 above are closed and the strict scan and manifest are green at
the final SHA, the single request will read:

> Approve production release of `<final SHA>` (tree `<tree>`) from base
> `ff3c496…`: deploy on Render; apply migration `e55f965f…` with precheck
> `742bce57…` and postcheck `7c7ef33b…` under stop-on-error;
> `RESEARCH_RESOURCE_HUB_ENABLED` remains absent; rollback = redeploy
> `ff3c496…`, schema retained; smoke = serving SHA, deploy ID, health on both
> origins, postcheck fingerprints, locked-gate Resources access and denials.
> Proposed executor: Claude, if granted Render and Supabase execution access
> for this action; otherwise Codex A on return.
