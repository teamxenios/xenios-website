# Resource Hub hardening qualification checkpoint

Pushed source: `8be5d582586217e4cf531e718c65032b79152022`; tree `6fc71d0a896d31d0843dd1eb63fa6265a3da9d28`. This source is not deployed or enabled. Production still serves `3814c687ef9293f84f939c372fdbc01b278a9193` as `dep-dag1reu7bikc73e16ie0`.

The successor binds recovered uploads to the winning file identity and makes review changes conditional on the expected state, full-precision provider timestamp, reviewer and reason. Rejected races preserve winner bytes, private residual objects and audit history. No scanner, SQL, production composition or permission change was added. Application source is unchanged after `62e6c63`; the final three added files provide a bounded managed-service harness.

Fresh exact-source gates passed: application plus explicit harness TypeScript, build, route uniqueness (433 registrations/424 call sites), protected core, and strict scan (4,636 added lines across 51 files; zero raw secrets, unresolved secrets or PII; no allowances or skipped scanning). Full suite: **16,086 passed, zero failed, 59 skipped and zero todo**, across 919 files with executed passing tests and five fully skipped files. Broad execution used Node 20.19.0, one worker, no file parallelism and an explicit 120-second per-test timeout from the earlier repair-batch procedure. This is not default-timeout equivalence. Build warnings remain in private logs.

Focused proof: 140 runtime cases, 56 harness cases, and 21 tool-captured UI cases passed. The pre-fix microsecond regression and earlier expanded-typecheck failures remain retained; the final source corrects them. Local provider doubles and synthetic UI tests do not prove managed behavior.

The supplemental source review found 14 covered paths, 37 formal unowned paths, no wrong-lane findings or conflicts, and exactly one active A lease for all 51 paths. **ASTRA-B exact-source and formal integration ownership acceptance remain pending.** Helper reviews are separately attributed.

## Managed qualification update at 17:18 UTC

The existing staging project `tetynodzrtmdbuzgboro` was restored once under bounded staging authorization. Exact e55 was installed once as20260908164502. Five synthetic Auth identities, three member rows, two partner rows and five initial password sessions were created; existing staging data and outbox2 remain preserved. The one approved benign PDF is private. See `STAGING_MANAGED_TEST_PLAN_20260908.md` and the installation/fixture receipts.

The first managed run stopped in preflight before writes because the harness incorrectly rejected the canonical sha256 projection. Its receipt and actual zero-effect SQL reconciliation remain preserved. Corrected harness `711bd4113b9a2458f7c9b46b50a088c8db728677`, tree `c5f59f8d15b022bd822fc0e6cb71233d1fad6c48`, passed88 focused tests, explicit TypeScript and independent helper review. Only three harness files differ from the unchanged8be application.

The fresh actual managed run passed41 checks at17:18:03Z. Fresh SQL reconciles four resources, two withdrawn versions, four private objects, two empty orphans, six delivery audits (one delivered/five denied), zero published pointers and unchanged outbox2. The proof covers full-precision timestamp round trips, controlled competing uploads/reviews, role refusals, approved-byte delivery, private orphan retention and withdrawal. It does not prove different-hash stored upload races or unfiltered retained-library behavior. See `managed-api-qualification-711bd411.json`; do not rerun the completed plan or recreate fixtures.

## Actual browser qualification at 18:01 UTC

The corrected host2873a333 and driver9d33ab53 ran against the unchanged application8be and preserved build. All15 prescribed real Auth/browser journey steps and45 role/width captures passed. Eight password sign-ins/eight normal UI sign-outs, byte-exact admin/partner downloads, pending-byte logout protection, same-context account switching and withdrawal were observed. Both hosts stopped gracefully with exit0; all203 provider requests have corresponding successful responses. Fresh SQL confirms five resources, three withdrawn versions, five private objects, eight delivery audits, zero published pointers and unchanged outbox2. The prior API data and failed runs remain preserved.

Independent helper reconciliation accepted the actual browser/host/raw-SQL receipts and all45 screenshot hashes/sizes/dimensions. This is bounded same-origin proxy qualification, not full production-shell proof;23 console-error events remain counted without raw classification, with zero page errors, boundary refusals or asset failures. See `managed-browser-qualification-9d33ab53.json` for exact identities, hashes, visual observations and limits.

## Remaining qualification

- Obtain actual ASTRA-B exact-source/ownership and managed/browser acceptance; helper reviews remain separately attributed.
- Before production activation, name the actual reviewed documents, audience and permitted verification effects. Private source-review documents and the synthetic staging PDF are not approved production content.

A's supported SQL connection returned PostgreSQL 17.6 and the three recorded migrations at `2026-09-08T15:47:12.09261Z`. Render confirms serving `3814`, auto-deploy off and the unchanged branch. The original account/partner release `ff3` was deployed on September 7; it must not be replayed. Existing installation postchecks, 30 unchanged critical endpoints and the 300,232 ms observation passed; rollback was unused. These are historical execution receipts, not new postchecks or real-user qualification.

Receipts: `hardening-qualification-8be5d582.json`, `closeout-actual-sql-render-20260908.json`, `supplemental-source-ownership-review-8be5d582.json`, and `hardening-record-validation-8be5d582.json`. The independently prepared `hardening-manifest-draft-8be5d582.json` contains the exact path and evidence hashes; it explicitly remains unqualified, with B attestation absent; its pending managed/browser fields now need reconciliation against the independently reviewed actual receipts. The original installation manifest and evidence remain unchanged. Full platform/revenue launch is not complete.
