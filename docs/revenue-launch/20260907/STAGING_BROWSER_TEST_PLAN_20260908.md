# Bounded real Auth browser qualification

ASTRA-A is the only managed-service executor. This plan implements the existing bounded staging authorization for `tetynodzrtmdbuzgboro`; it does not authorize production activity. The application is unchanged `8be5d582586217e4cf531e718c65032b79152022`, tree `6fc71d0a896d31d0843dd1eb63fa6265a3da9d28`. Host and driver source, their private execution plans, the preserved build, and served assets must be independently pinned before execution. B acceptance remains a release gate.

Use the existing five synthetic identities. No further identity, application, member or partner is created. Two isolated hosts use canonical admin guards with fixed adminA and adminB configuration. Auth requests from the actual client go through a loopback proxy to real staging GoTrue, allowing bounded attempts to be journaled before transport. No fake identity response, injected session, invitation, notification or job is used. Direct-origin production Auth and incidental full-shell endpoints are outside this proof.

The combined maximum is eight normal password sign-ins and eight canonical UI sign-outs, with zero token refreshes. Host A allows adminA twice, adminB once, eligible twice, nonPartner once and suspended once. Host B allows adminB once. The admin uses the application's global/default sign-out and members use its local sign-out. Counts must reconcile between browser requests and host receipts. Stop on unexpected refresh, identity, destination, response or budget.

Host A may create one additional resource, version and private object using only the existing benign PDF: 2,333 bytes, SHA-256 `9bf66f0c5ed7ce28d90cb0dca3a3402067a0b8df61a00a0ab8f833fc5f03a198`. Its usage policy is private and audience is research_rep. Permit one request-review update, one approval update, one publication, one withdrawal, two eligible delivery audits and two admin previews. Canonical generated IDs and the upload key are bound and journaled before the first effect. Host B has zero Hub writes and observes only its retained run-owned fixtures. Preserve the four earlier API objects/resources, two withdrawn versions and six audits.

Capture nine widths (320, 360, 390, 414, 768, 1024, 1280, 1440 and 1920) for five states: authorized admin, eligible partner, non-partner, suspended partner and forbidden admin. Reuse authenticated pages for all 45 captures. Verify normal byte-exact partner download and admin preview. For each pending-byte logout check, observe a real successful byte response queued at the host before sign-out; the host delays it four seconds. Prepare the member's second account tab before starting the pending download, leaving the Resources page mounted. Verify no stale save and actual account switching in the same browser context.

Browser denials cover the displayed access and empty-library states. Direct denied-download and competing-upload/review assertions remain attached to the completed 41-check managed API proof; they are not repeated or charged to this browser budget. The browser's final withdrawal check verifies the eligible library is empty.

All evidence, screenshots, keys and passwords remain outside Git. Default preflight reads no passwords and starts no browser. Execution requires exact private-plan hashes and unchanged source/build pins. Capture only fixed failure codes, stages and counters; no raw exception, console, request-body, token, trace or HAR output. Review private screenshots before any publication.

After failure, stop the two hosts using their explicit graceful stdin control. Freeze transport, abort and drain requests, prevent late callbacks from writing after receipt closure, and preserve all effects. Do not retry uncertain writes, automatically sign out again, delete objects or reset staging. Reconcile actual SQL and host/browser receipts before any further action. Real production content, audience and user-verification effects need their separate concrete decision.
