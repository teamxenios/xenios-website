# Resource Hub activation fix comparison

Independent read-only review by the `release_manifest` agent compared exact source `d974d2612ff914045761340d06243a3ef40976d2` and alternate `6af2474357f31861200efd6be02624a4a8e8986b`, both based on `3f51ed4af75e47ef5c94ab18a46a9f2d4fc27b9e`. This is additional source review, not ASTRA-B acceptance or managed-service proof.

Both recover an upload-key winner only when its SHA and filename match. The d974 implementation additionally checks reviewer and review reason alongside state and review time during one conditional persistence update. It also treats a missing memory row as the same typed conflict as the managed adapter. Keep these protections and selectively adapt useful alternate service-level stale-snapshot tests; do not wholesale replace the source or newer production records.

Both proposed candidates have a remaining precision defect: `toVersion` maps `reviewed_at` through JavaScript Date, discarding PostgreSQL microseconds before the conditional update compares it. A valid non-null review can therefore produce a false conflict. The minimal correction preserves the raw provider timestamp only for `reviewedAt`; the existing string-or-null DTO and display formatter support it. Other date mappings and migration bytes remain unchanged. The builder is correcting this on its isolated branch and adding null, millisecond and microsecond read-to-update regression coverage, retaining the failing pre-fix result.

Neither prior test count qualifies that changed source. The corrected successor needs its own affected/integrated tests, typecheck/build, strict source scan, exact manifest and independent source review. Actual managed Auth/PostgREST/Storage and role/logout/delivery proof remain separate requirements. The public production installation at3814 remains disabled and unchanged. Upload object/resource writes can precede an insert conflict; preserved orphans are not described as zero writes. No fixture is to be created in production for these checks.
