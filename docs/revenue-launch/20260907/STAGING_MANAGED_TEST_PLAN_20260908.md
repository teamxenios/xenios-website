# Bounded staging Resource Hub qualification

ASTRA-A owns service writes. Target: `tetynodzrtmdbuzgboro` only. Application source: `8be5d582586217e4cf531e718c65032b79152022`, tree `6fc71d0a896d31d0843dd1eb63fa6265a3da9d28`. The three-file harness extension is separately source-bound and still being completed; no managed workflow result is claimed here. B acceptance and browser proof remain required.

Staging was restored once on the existing Free plan. Exact e55 Hub migration installed once as `20260908164502`; complete unchanged pre/postchecks finished without error. Existing Auth user/application, two outbox rows and six prior private buckets remain; the seventh bucket is the new private Hub bucket. No core migration, history repair or reset occurred.

## Private fixture plan

Run: `hubqa-20260908-bdda04a633`. Private provisioning-plan SHA-256: `551728acede73dba767bc307211c1523565550f8f9dc6680478c1476abaae032`. All identity addresses use the reserved `.invalid` domain. Passwords, project keys, sessions and identity values remain outside Git and tool output.

Exactly five synthetic Auth identities: adminA, adminB, eligible, nonPartner and suspended. The admins have no member/partner record; two isolated application processes each use the unchanged canonical admin guard. The other three have active member rows with billing_state=not_started. Eligible and suspended each have a research_rep partner fixture, with active and suspended states respectively. Fixture clearances are synthetic test data and do not assert real identity/tax/payment verification.

Provisioning ceiling: five Auth creates, three application inserts, three member inserts, two partner inserts and five initial password sign-ins. Admin createUser uses email_confirm=true and authenticated JWT role, with no invitation, signup-email, reset, magic-link or OTP call. The inspected project has no Auth hooks, seven disabled security email switches, no custom SMTP, no cron/net/http extensions, no Edge Functions and no relevant Auth/core user triggers. No application/job process runs during provisioning.

## One PDF and managed effects

The single document is `xenios-staging-qualification.pdf`: 2,333 bytes, one page, SHA-256 `9bf66f0c5ed7ce28d90cb0dca3a3402067a0b8df61a00a0ab8f833fc5f03a198`. It contains only a synthetic staging workflow description. The unchanged application scanner returned ok=true with no reasons. Poppler rendered the page; visual inspection found it legible and complete. An unavailable PyMuPDF import was replaced by the installed Poppler renderer; the PDF bytes were not changed.

Use the same bytes for both resources and all concurrent upload attempts. usagePolicy=private. The control resource targets affiliate; the journey resource targets research_rep. A second simple losing filename tests filename disagreement using the same PDF bytes. No second document/hash is approved or uploaded. This does not establish a different-byte managed race result.

Planned managed ceiling, subject to the final independently reviewed harness: four objects/four resources; four version insert attempts yielding two versions and two expected unique conflicts; seven review/precision attempts yielding four updates, two zero-row conflicts and one provider rejection; two publish and two withdraw transitions; six delivery audits. All test content remains in private Storage. Retained-content reads are scoped to run-owned identifiers and this limitation must accompany the result.

Required proof: upload recovery, full-precision timestamp round trip, matching/stale review expectations, competing administrators, intended audience visibility, correct downloaded hash, unauthenticated/non-partner/suspended/outside-audience denial, withdrawal, unpublished private orphans and exact audit reconciliation. Real rendered logout/account switching uses the separate bounded STAGING_BROWSER_TEST_PLAN_20260908.md. A freezes its source and private execution plans after independent helper review under the current instruction to run the approved staging journey; formal B source/ownership and browser acceptance remain release gates. An API run is not browser qualification.

## Preservation and stop conditions

Write the exact plan and a durable attempt journal before each transport call. Stop on any unexpected failure, identity/target/hash mismatch, unknown write outcome or budget violation. Preserve every fixture, object, session receipt and audit record. After interruption, query actual run-owned state before any retry; do not automatically replay provisioning or the harness. Withdraw only identified test publications under the reviewed recovery procedure if required. No deletion, reset, migration-history repair or unrelated schema change is authorized.

Production remains at the separately recorded3814 installation with Hub disabled. This plan does not publish the synthetic PDF or private source-review documents in production, grant a real account, notify a customer or perform commerce, payment, shipment or clinical actions. Actual production content, audience and permitted verification effects require a reviewed decision before activation.
