# Native finish: subordinate path assignments

Integration owner: `codex-native-finish-20260910` in the isolated
`codex/xenios-native-finish-20260910` worktree. Production authority remains
limited to the authentic recorded approvals; this document grants none.

## Completed bounded delegation

`/root/native_finish_review` was assigned only:

- `server/research/commerce/qualification/native-sql-rehearsal.mjs`

Assignment: a disposable, in-memory PostgreSQL rehearsal runner using the exact
checked-in SQL, including retained negative controls. No dependency changes,
remote calls, SQL source edits, competing heavy tests, or production actions.
The integration owner reviewed the returned artifact and ran it: 23/23 local
checks passed, including all eight SQL files and 11 retained negative controls.
The worker did not self-accept its own implementation. Its write assignment is
now released back to the integration owner. Managed qualification remains open.

All other current integration paths remain with the main owner. Claude's active
worktree is preserved and consumed selectively after a checkpoint; it is not
another integration authority. No permanent worker or external session is created.

## Credit reservation slice (2026-09-10)

Main owns candidate `20260910201400_research_checkout_credit_reservations*`,
execution/submission integration and SQL qualification. The candidate name was
generated by the installed Supabase CLI into an isolated local scaffold; no
remote migration or history operation was performed.

Bounded worker `/root/product_review_filters` owns only
`server/research/commerce/persistence/store-credit-store.ts` and its adjacent
`store-credit-store.test.ts` for the complete-balance RPC and serialized spend
adapter contract supplied by main. No other paths, production effects or heavy
runs are delegated. Main independently reviews/integrates the result.

Worker `/root/native_finish_review` owns only the new
`server/research/commerce/qualification/credit-reservation-sql-rehearsal.mjs`:
a local disposable exact-SQL regression runner, not SQL source or production.
Main reviews and executes it; worker may not self-accept the release contract.

Both code assignments are now returned to main after artifact review and local
execution. The worker's current optional runtime-download assignment is confined
to `C:/Users/sboad/projects/xenios-native-finish-evidence-20260910/postgres17-runtime/`;
no service installation, repository dependency change or database start is delegated.

The portable runtime assignment is complete and returned. Main initialized and
stopped the exact synthetic local cluster. `/root/product_review_filters` then
owned only the external evidence artifact `credit-concurrency/pg17-credit-proof.mjs`;
main reviewed and executed it, and `/root/native_finish_review` independently
accepted the final local result. Both write assignments are now returned to main.
Receipt operational integration discovery is read-only; it grants no new file
ownership, notification effects or production authority.
