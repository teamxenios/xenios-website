# Referral V1 browser smoke diagnostics

## Current checkpoint: functional 320-pixel smoke passed

`browser-smoke-c5ac439-320/browser-results.json` records a functional local synthetic PASS at exact source SHA `c5ac43907ffe024ffcba847fb761836211ae4118`. Initial and final source-tree hash `6ade325985c1df208b8448e75be7bab0e4f83e409f6d5ca83e33e1b731659b7b` and whole-bundle hash `5069e6fb51045747878ca29acea5496566a0dce5a70e62acd04b169d246cc464` match. There are 20 screenshots, 495 exact-loopback requests, zero external requests, zero boundary violations, zero runtime exceptions, and confirmed Chromium/profile plus preview/database cleanup.

Owner create/copy/simulated share/fallback/copy failure/revoke, invalid/revoked/expired recipients, explicit Care/Research continuation, exact auth returns, ordinary-member authorization denials, password recovery, account claim and admin lifecycle all completed. Real database telemetry proves no recipient binding before normal sign-in; no recovery binding during recovery or after reset before fresh sign-in; and no claim binding immediately after claim. Each persona then has exactly one binding to its expected UI-created link after normal sign-in. The claimed account remains at `/research/activate`; the actual disabled activation read does not grant membership or payment authority.

Final actual database counts were 8 links, 3 touches, 3 bindings and 15 events. Request/order lineage was inserted separately as explicit synthetic fixtures for three already-bound accounts and shown in admin. This is not evidence of request submission, order conversion or independently verified order-level referral attribution. Native share/clipboard branches remain simulated browser capabilities, not OS delivery evidence.

This is **not mobile visual acceptance**. Inspected owner, Health recipient, recovery, activation-gated claim and admin screenshots retain the existing PWA promotion. It obscures owner link details, the Health Research choice, activation-status explanation and admin's first lifecycle heading; it also occupies the recovery page's lower area. No new horizontal overflow or below-44-pixel target was reported in the asserted flow controls, but those geometry checks do not prove absence of occlusion. The full nine-width matrix is deliberately held until the separately assigned PWA policy change and rebuilt final candidate. No production verification, deployment or migration approval is implied.

## Preserved failed attempts

These are preserved failed local synthetic runs, not browser acceptance or production evidence. All four failed runs confirmed owned Chromium/profile and preview/database cleanup.

| Run | Exact source SHA | Stop condition | Disposition |
| --- | --- | --- | --- |
| `browser-smoke-c9c81bb-320` | `c9c81bbc62429b09543e858dff2b6779684232f4` | Public browse reached `/research/` instead of `/research`. | Preview used default static-directory redirects. It now matches canonical `server/static.ts` with `redirect: false`; the public destination assertion is exact again. |
| `browser-smoke-3a3d1ee-320` | `3a3d1eeeb63154e73c0de824e457ce3189abcebc` | One Manifest request remained pending during the next programmatic navigation. | Failed attempt retained. Harness now waits for each clicked destination's document completion and the unchanged strict network quiet check before another navigation; added loader/lifecycle diagnostics. No request filtering or service-worker bypass. |
| `browser-smoke-05e285c-320` | `05e285c7cbce2b7b0b625b4c93ce851601696b80` | After explicit Care capture and navigation to `/care`, six current-loader Fetch requests remained pending: five `/api/waitlist/count` and one `/api/care/access-request/status`. Each had received a 404 response, not a loading-finished event. | Preview's API firewall omitted these read-only public shell dependencies. Separately identified future full-catalog and activation-status reads before another freeze. No production change or acceptance inferred. |
| `browser-smoke-ef6c616-320` | `ef6c6165c66a53771fb2b9a410d26e2a43ad06cc` | Opening the synthetic recovery URL from the identical reset-request document changed only its hash; no new-document load event fired. No requests remained pending. | Care and catalog now settled; actual recipient sign-in preserved the destination and created exactly one expected binding after zero before sign-in. Principal-isolation negatives passed. Harness will open the synthetic email URL from a separate local document, preserving the visitor cookie, without forcing recovery success. |

The fourth report's two error fields were subsequently redacted to remove the disposable synthetic recovery credential URL's query/fragment while retaining the original failure and stack meaning. The failed result, source and bundle fingerprints, checks, screenshots and cleanup results were not changed. Centralized error sanitization now covers report and CLI failure output; the previously emitted console line cannot be retroactively changed.

For the third run, all ten `/site.webmanifest` requests received HTTP 200, `fromServiceWorker: false`, and `Network.loadingFinished`. There was no pending Manifest request. All 175 observed requests used the exact loopback preview origin; external HTTP requests, boundary violations and runtime exceptions were zero.

The current-loader 404 response bodies are not consumed in `use-waitlist-count.ts` and `CareAccessRequestForm.tsx` before those clients return or throw. That is a source-backed explanation consistent with the pending-request telemetry, not proof from changing the browser's network state. The general research API adapter similarly returns on 404/501/503 before consuming the body; the downstream full catalog and activation reads require careful handling. No idle timeout was raised and no inflight request was removed to make a run pass.

Owner create/copy/simulated-share/fallback/copy-failure/revoke and invalid/revoked/expired recipient contexts ran before the third stop. Auth binding, reset, claim and admin lifecycle had not yet run in this smoke, so those browser flows are not verified by it. Their separate unit/HTTP/database evidence must not be substituted for browser completion.

The existing PWA install promotion visibly overlaps owner link details and the Health recipient choice at 320 pixels in earlier inspected screenshots. Geometry and minimum-target assertions do not detect this occlusion. The promotion was not hidden or dismissed for evidence; visual/mobile acceptance remains separate from any eventual functional matrix result.
