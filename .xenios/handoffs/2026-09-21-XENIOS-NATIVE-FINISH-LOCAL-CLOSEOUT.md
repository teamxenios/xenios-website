# Native finish — local closeout handoff

TASK: `XENIOS-NATIVE-FINISH-20260910`

SESSION: `codex-native-closeout-20260921`

BASE SHA: Claude's preserved clean, pushed
`2b776f942aeb270fd0766ec036a2c275fa2e094d`.

BRANCH: `codex/xenios-native-closeout-20260921`

APPLICATION SHA: `6d1792c82a681b7537b6fefa3581b2c95a224bb4`, tree
`2e03bcf6cf103cd4aa6d99351ad7f2301b66ea2c`.

TEST SUCCESSOR: `c559c4e7795fb211b082cf12bd791f93c1cd3851`. Only a test-only
cleanup expectation and two reviewed-fixture JSON records changed above the
application candidate. Runtime bytes are identical.

STATUS: Local engineering qualified; managed release blocked. The current
source lease is released and the task remains `blocked_external`, not complete.
This is not production acceptance or authorization.

FILES CHANGED / WHAT WAS BUILT: See the bounded source-change table and exact
receipts in `docs/release-candidate/LOCAL_CLOSEOUT_20260921.md`. This continuation
closed actual DTO/read-state defects, native/admin browser loops, member-owned
XRR history/status and authorization gaps, navigation truth and responsive
targets, plus guarded local SQL candidates. Original worktrees were preserved.

WHAT WAS NOT BUILT: No speculative EA money-grant writer, second fulfillment
authority or legacy status fallback. No hosted checkout/provider qualification,
managed migration installation, deployment or clinical/provider activation.

QUALIFICATION: Exact clean build passed, 84 native authenticated captures and
120 public/signed-out captures passed, extended API journey passed, and disposable
PostgreSQL checks passed (Referral 12, XRR 13). Full suite: 17,896 tests passed,
71 explicitly skipped, zero failed. Application and release typechecks,
route/DAG/protected seams and the strict bounded diff scan passed. The static
production-state verifier failed closed on absent trusted release/ownership
inputs and stale evidence. Full hashes are in the closeout/continuation receipts.

SECURITY / PRIVACY: Two canonical SQL NULL-authorization bypasses were reproduced
on fresh local PostgreSQL, not observed live. The new guarded candidate fixes
the legacy denial and supplies service-only history/status readers. The strict
scan uses the unchanged approved private V3 input and an independently reviewed
exact fixture registry; no private names are checked in. Local simulations are
not managed-service attestations. Failed and interrupted runs remain preserved.

MIGRATION: Both `20260914_research_referral_v1_touch_attribution.sql` and
`20260921_research_assisted_order_member_history.sql` are unapplied candidates.
Qualify the actual managed predecessor and exact installation/postcheck packet.
Do not replay canonical migrations or roll back to the unsafe NULL guard.

FEATURE FLAGS: Unchanged. PRODUCTION MUTATED: No. Production identity/schema
was not freshly observed; September 14 records remain historical.

INTEGRATION INSTRUCTIONS: Review this branch and the report. Its status/history
reads intentionally become unavailable when the new RPCs are absent. New SQL
and application release must therefore be coordinated through the authorized
managed release process. Preserve fail-closed behavior instead of restoring the
legacy fallback for compatibility. Keep the original Claude branch intact.

KNOWN RISKS / NEXT EXTERNAL ACTION: Fresh trusted release/ownership inputs and
production observations must close the failed production-state gate. A named
executor must qualify the exact two
SQL candidates and the separate hosted checkout candidate-2 amendment with
staging/test-provider credentials and synthetic identities. The EA grant writer
requires the canonical mapping from a partner to that partner's own EA customer
record, plus server-owned hold-rate and idempotence authority. Do not infer them
from a partner UUID or browser values. These inputs have been requested; none
was supplied during this closeout. Clinical/provider activation is separate.

EVIDENCE ROOT: `C:/Users/sboad/xenios-recovery/codex-native-closeout-20260921`.
Browser, local database and regression processes have completed or stopped.
No background work is being left running as a substitute for qualification.
